# aws-resource-tracker
A simple Bash script that audits your AWS account by reporting active resources across EC2, S3, Lambda, and IAM. Uses the AWS CLI and jq to pull clean, readable output, built for quick resource tracking and cost/usage awareness.
